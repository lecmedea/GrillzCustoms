# Studio assets

- `assets/studio/teeth/*.png` — photorealistic PNG layers (kind × material)
- `assets/studio/overlays/*.png` — style overlays
- `assets/studio/models/gc-grillz-studio.glb` — original GC GLB for model-viewer / AR
- `data/gc-prices.json` — official GC per-tooth price list

Regenerate: re-run the asset builder from the Grok session or extend with Blender for higher-fidelity GLB.
